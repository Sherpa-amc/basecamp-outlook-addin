/* Required Office.js initialization for the commands surface.
 * No function commands are currently defined — the ribbon button uses
 * ShowTaskpane (declared in manifest.xml). This file exists because the
 * Office runtime requires the FunctionFile URL to load successfully. */
Office.onReady(() => {
    // No-op
});
